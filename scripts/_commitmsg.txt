chore(catalog): wire community local_rel + harden scripts + delete orphan thumbs

Repository hygiene:

- Wired up local_rel on 138/139 community records (was: 1/139). The
  downloaded previews in assets/community/superdesign/<slug>/preview.png
  now resolve correctly. local_kind auto-detected (124 png, 14 mp4, 1 jpeg).
  Result: visual bucket counts go from {videos:107, images:173, concepts:224}
  to {videos:120, images:297, concepts:86} - 138 cards now show their
  intended preview instead of falling back to a concept card.

- Deleted 8 orphan thumbnails (404, aetheris-voyage-hero, ai-image-generator-ui,
  asme-hero, modern-hr-dashboard, prisma-landing, saas-value, social-hangouts)
  that were never referenced by the catalog (-5.3 MiB).

- Added scripts/lib/catalog-utils.js :: findOrphanAssets(root) so the audit
  now flags any future unreferenced preview under assets/previews or
  assets/community as 'orphan-asset' (2 new tests).

Robustness:

- Hardened scripts/lib/category-utils.js :: normalizeCategory to accept
  non-string inputs without crashing. Booleans become 'Yes'/'No', finite
  numbers pass through, NaN/Infinity/objects/arrays return '' instead of
  throwing or producing '[object Object]'. (2 new tests.)

- scripts/normalize-categories.js now has exit codes: 0 ok, 2 missing
  input file, 3 malformed JSON. Supports --quiet. Also surfaces a WARN if
  distinct categories grew (which would mean a regression in the map).

Tests: 30/30 pass (was 26, +4 new tests for orphan + non-string inputs).
Audit: AUDIT OK records=504 complete=476 community=139 missingBodies=28 assets=418.
Build: SELF-VERIFY OK records=504, 297 images / 120 videos / 86 concepts.
